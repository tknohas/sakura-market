%h2.text-center.mb-4 商品一覧

.container
  .row
    - @products.each do |product|
      .col-md-4.mb-3
        .card
          .card-body
            .d-flex.align-items-start
              .me-3
                = render 'shared/product_image', product: product, size: '120x80', variant: :small
              .flex-grow-1
                .d-flex.justify-content-between.align-items-start
                  .me-2
                    %h5.card-title= link_to product.name, product_path(product)
                    %p.card-text
                      価格: #{number_to_currency(product.price)}
  .d-flex.justify-content-center
    = paginate @products, theme: 'bootstrap-5'
